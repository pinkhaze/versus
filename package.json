{
  "name": "versus",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js --ignore client",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/barcar95/versus.git"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "bugs": {
    "url": "https://github.com/barcar95/versus/issues"
  },
  "homepage": "https://github.com/barcar95/versus#readme",
  "dependencies": {
    "apollo-server-express": "^3.12.1",
    "bcrypt": "^5.1.1",
    "graphql": "^16.8.1",
    "jwt-decode": "^3.1.2",
    "mongoose": "^7.6.1",
    "react-router-dom": "^6.16.0",
    "react-scripts": "^5.0.1"
  }
}
